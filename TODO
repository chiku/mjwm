* Avoid double extensions (.png, .xpm)
* Cleanup Makefile.am & configure.ac
* Update jwmrc when supplied option
* Change architecture - list of desktop files should convert to JWM menu when send a JWM menu visitor
* FIX REGRESSION - mjwm -i path adds new files to the one already read from XDG env. variables. Fix before next release
* Add deprecation for -h & -v command line options. Drop in next major release
* Cleanup commmand_line_options error test - see if parsing can be done using vanilla C++
